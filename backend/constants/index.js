/**
 * Constants Index
 * Centralized constants for the PokéChat backend
 */

const HTTP_STATUS_CODES = require('./httpStatusCodes');
const ERROR_MESSAGES = require('./errorMessages');

module.exports = {
  HTTP_STATUS_CODES,
  ERROR_MESSAGES
};
